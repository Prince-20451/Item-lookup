{
  "name": "item-lookup",
  "version": "1.0.0",
  "private": true,
  "description": "Zoho Books item lookup with a server-side proxy that keeps Zoho credentials out of the browser.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
